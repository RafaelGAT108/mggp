# Getting Started - MGGP

##	Installation Software dependencies 
All basic requirements are in the file `requirements.txt`. To install everything:
```shell
 pip install .
```


The main file of F-16 MIMO is `f16_mimo.py`